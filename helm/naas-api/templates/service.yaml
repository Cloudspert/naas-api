apiVersion: v1
kind: Service
metadata:
  name: {{ include "naas-api.fullname" . }}
  labels:
    {{- include "naas-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "naas-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
