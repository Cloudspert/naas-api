{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "naas-api.serviceAccountName" . }}
  labels:
    {{- include "naas-api.labels" . | nindent 4 }}
{{- end }}
